"""
This package used to contain code to detect positions from images, before we used a neural network for that.

Nowadays, just the code for fitting 3D Gaussian functions to the images remains. The code is quite slow, so maybe it
should be removed at some point.
"""
